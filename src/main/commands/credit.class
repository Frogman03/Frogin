package com.firstplugin;

import org.bukkit.command.command;
import org.bukkit.comand.commandexecurtor;
import org.bukkit.command.commandsender;

public class credit implements commandexecurtor {

    @Override
    public boolean command(commandsender commandsender, command command, string label, string[] args) {
       if (!(sender instanceof player))
       player.sendmessage = "This message is intended to be sent by a player only!"
       
        if (sender instanceof player) 
            player player = (player) sender; 

    // command
    if (sender = player)
      if (cmd.getname().equalsignorecase(commandexecutor "tell"))
        player.sendmessage(s: "Credit to frogman")

        